#include<stdio.h>

int main()
{
  int iNo1=0,iNo2=0,no;
  printf("\nenter first number=%d",iNo1);
  scanf("%d",&iNo1);
  printf("\nenter second number=%d",iNo2);
  scanf("%d",&iNo2);
      
  no=iNo1;

  iNo1=iNo2;  

  iNo2=no;  
 
  printf("\n after swaping first number=%d",iNo1);
  
  printf("\n after swaping second number=%d",iNo2);
   return 0;
}
    